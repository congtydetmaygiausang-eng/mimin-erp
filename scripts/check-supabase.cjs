// Check tất cả bảngconst fs = require('fs');const path = require('path');const envPath = path.join(__dirname, '..', 'apps', 'web', '.env.local');const envContent = fs.readFileSync(envPath, 'utf8');const url = envContent.match(/NEXT_PUBLIC_SUPABASE_URL=(.+)/)[1].trim();const pubKey = envContent.match(/NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=(.+)/)[1].trim();async function check(tbl) {  const r = await fetch(`${url}/rest/v1/${tbl}?select=*&limit=1`, {    headers: { apikey: pubKey, Authorization: `Bearer ${pubKey}` },  });  const text = await r.text();  let parsed;  try { parsed = JSON.parse(text); } catch { parsed = text; }  if (r.status === 200) {    const cols = Array.isArray(parsed) && parsed[0] ? Object.keys(parsed[0]) : [];    return { tbl, status: 200, msg: 'OK', cols };  }  return { tbl, status: r.status, msg: parsed?.message || parsed?.hint || text.substring(0, 150) };}(async () => {  const tables = [    'cong_nhan_gia_cong',    'lenh_cat',    'san_pham',    'nhan_su',    'cong_no',    'doi_tac',    'kho_vai',    'phieu_chi',    'phieu_thu',  ];  for (const t of tables) {    const r = await check(t);    if (r.status === 200) {      console.log(`✅ ${r.tbl}: OK (${r.cols.length} cols: ${r.cols.slice(0, 6).join(', ')}${r.cols.length > 6 ? '...' : ''})`);    } else {      console.log(`❌ ${r.tbl}: ${r.status} - ${r.msg.substring(0, 100)}`);    }  }})();