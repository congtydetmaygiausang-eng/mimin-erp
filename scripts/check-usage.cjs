// Test query agent_usage_logsconst fs = require('fs');const path = require('path');const env = fs.readFileSync(path.join(__dirname, '..', 'apps', 'web', '.env.local'), 'utf8');const url = env.match(/NEXT_PUBLIC_SUPABASE_URL=(.+)/)[1].trim();const key = env.match(/NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=(.+)/)[1].trim();(async () => {  // Test 1: query bang agent_usage_logs voi anon key  console.log('Test 1: GET /rest/v1/agent_usage_logs?select=*&limit=1');  const r = await fetch(`${url}/rest/v1/agent_usage_logs?select=*&limit=1`, {    headers: { apikey: key, Authorization: `Bearer ${key}` },  });  console.log('Status:', r.status);  const text = await r.text();  console.log('Body:', text.substring(0, 200));  // Test 2: query voi headers Accept  console.log('\nTest 2: GET with Prefer: count=exact');  const r2 = await fetch(`${url}/rest/v1/agent_usage_logs?select=id&limit=0`, {    headers: { apikey: key, Authorization: `Bearer ${key}`, Prefer: 'count=exact' },  });  console.log('Status:', r2.status);  console.log('Body:', (await r2.text()).substring(0, 200));})();