const { createClient } = require("@supabase/supabase-js");

const supabaseUrl = "https://mock.supabase.co"; // wait, the user's supabase is real. Let me grep for NEXT_PUBLIC_SUPABASE_URL in .env.local
