const fs = require('fs');
const file = 'apps/web/src/app/(main)/kho-thanh-pham/page.tsx';
let code = fs.readFileSync(file, 'utf8');

// 1. Add Video import
code = code.replace(/Camera, PackageOpen, Save } from "lucide-react";/, 'Camera, PackageOpen, Save, Video } from "lucide-react";');

// 2. Add productVideos and uploadType state
code = code.replace(
  `const [productImages, setProductImages] = useState<Record<string, string>>({});\n  const [uploadingSP, setUploadingSP] = useState<string | null>(null);\n  const fileInputRef = useRef<HTMLInputElement>(null);`,
  `const [productImages, setProductImages] = useState<Record<string, string>>({});\n  const [productVideos, setProductVideos] = useState<Record<string, string>>({});\n  const [uploadingSP, setUploadingSP] = useState<string | null>(null);\n  const [uploadType, setUploadType] = useState<"image" | "video">("image");\n  const fileInputRef = useRef<HTMLInputElement>(null);`
);

// 3. Update handleFileChange
code = code.replace(
  `setProductImages((prev) => ({\n          ...prev,\n          [uploadingSP]: ev.target?.result as string,\n        }));`,
  `if (uploadType === "video") {\n          setProductVideos((prev) => ({ ...prev, [uploadingSP]: ev.target?.result as string }));\n        } else {\n          setProductImages((prev) => ({ ...prev, [uploadingSP]: ev.target?.result as string }));\n        }`
);

// 4. Update fileInput to accept video
code = code.replace(
  `<input type="file" ref={fileInputRef} className="hidden" accept="image/*" onChange={handleFileChange} />`,
  `<input type="file" ref={fileInputRef} className="hidden" accept="image/*,video/*" onChange={handleFileChange} />`
);

// 5. Add Video thumbnail and Action buttons to the Master Card
const oldHeader = `<div \n                        className="w-20 h-20 md:w-28 md:h-28 bg-black/20 rounded-xl flex-shrink-0 relative overflow-hidden group cursor-pointer"\n                        onClick={() => { setUploadingSP(group.maSP); fileInputRef.current?.click(); }}\n                      >\n                        {productImages[group.maSP] ? (\n                          <img src={productImages[group.maSP]} className="w-full h-full object-cover group-hover:scale-110 transition-transform" alt={group.tenSP} />\n                        ) : (\n                          <div className="absolute inset-0 flex flex-col items-center justify-center">\n                            <Camera className="w-6 h-6 mb-1 opacity-60 group-hover:opacity-100 group-hover:scale-110 transition-all" />\n                            <span className="text-[9px] font-bold uppercase tracking-wider opacity-80">Tải ảnh</span>\n                          </div>\n                        )}\n                      </div>\n                      <div className="flex-1 w-full">\n                        <div className="flex items-start justify-between">\n                          <div>\n                            <div className="inline-block px-2 py-0.5 bg-white/20 rounded text-[10px] font-bold uppercase tracking-wider mb-1.5 backdrop-blur">{group.maSP}</div>\n                            <h2 className="text-xl md:text-2xl font-bold leading-tight mb-3">{group.tenSP}</h2>\n                          </div>\n                        </div>`;

const newHeader = `<div className="flex gap-2 flex-shrink-0">\n                        <div \n                          className="w-20 h-20 md:w-28 md:h-28 bg-black/20 rounded-xl relative overflow-hidden group cursor-pointer border border-white/20"\n                          onClick={() => { setUploadingSP(group.maSP); setUploadType("image"); fileInputRef.current?.click(); }}\n                          title="Đổi ảnh bìa"\n                        >\n                          {productImages[group.maSP] ? (\n                            <img src={productImages[group.maSP]} className="w-full h-full object-cover group-hover:scale-110 transition-transform" alt={group.tenSP} />\n                          ) : (\n                            <div className="absolute inset-0 flex flex-col items-center justify-center">\n                              <Camera className="w-6 h-6 mb-1 opacity-60 group-hover:opacity-100 group-hover:scale-110 transition-all" />\n                              <span className="text-[9px] font-bold uppercase tracking-wider opacity-80 text-center leading-tight mt-1">Ảnh<br/>bìa</span>\n                            </div>\n                          )}\n                        </div>\n                        <div \n                          className="w-20 h-20 md:w-28 md:h-28 bg-black/20 rounded-xl relative overflow-hidden group cursor-pointer border border-white/20"\n                          onClick={() => { setUploadingSP(group.maSP); setUploadType("video"); fileInputRef.current?.click(); }}\n                          title="Đổi video"\n                        >\n                          {productVideos[group.maSP] ? (\n                            <video src={productVideos[group.maSP]} className="w-full h-full object-cover group-hover:scale-110 transition-transform" muted loop autoPlay playsInline />\n                          ) : (\n                            <div className="absolute inset-0 flex flex-col items-center justify-center">\n                              <Video className="w-6 h-6 mb-1 opacity-60 group-hover:opacity-100 group-hover:scale-110 transition-all" />\n                              <span className="text-[9px] font-bold uppercase tracking-wider opacity-80 text-center leading-tight mt-1">Video<br/>SP</span>\n                            </div>\n                          )}\n                        </div>\n                      </div>\n                      <div className="flex-1 w-full">\n                        <div className="flex items-start justify-between">\n                          <div>\n                            <div className="inline-block px-2 py-0.5 bg-white/20 rounded text-[10px] font-bold uppercase tracking-wider mb-1.5 backdrop-blur">{group.maSP}</div>\n                            <h2 className="text-xl md:text-2xl font-bold leading-tight mb-3">{group.tenSP}</h2>\n                          </div>\n                          <div className="flex gap-1.5 hidden md:flex">\n                            <button onClick={() => alert('Xem chi tiết: ' + group.tenSP)} className="p-1.5 bg-white/10 hover:bg-white/20 rounded-lg backdrop-blur text-white transition-colors" title="Xem chi tiết">\n                              <Eye className="w-4 h-4" />\n                            </button>\n                            <button onClick={() => setShowAdd(true)} className="p-1.5 bg-white/10 hover:bg-white/20 rounded-lg backdrop-blur text-white transition-colors" title="Thêm biến thể mới">\n                              <Plus className="w-4 h-4" />\n                            </button>\n                            <button onClick={() => alert('Chức năng sửa thông tin sản phẩm mẹ')} className="p-1.5 bg-white/10 hover:bg-white/20 rounded-lg backdrop-blur text-white transition-colors" title="Sửa sản phẩm">\n                              <Edit className="w-4 h-4" />\n                            </button>\n                            <button onClick={() => { if(confirm('Xóa toàn bộ sản phẩm này?')) update(dsSanPham.filter(s => s.maSP !== group.maSP)); }} className="p-1.5 bg-rose-500/80 hover:bg-rose-500 rounded-lg backdrop-blur text-white transition-colors" title="Xóa toàn bộ sản phẩm">\n                              <Trash2 className="w-4 h-4" />\n                            </button>\n                          </div>\n                        </div>`;

code = code.replace(oldHeader, newHeader);

// 6. Update LSX input in AddEditModal to auto-fill
const oldLsx = `<input value={form.lsx} onChange={(e) => setForm({ ...form, lsx: e.target.value })} className="w-full px-3 py-2 border-2 border-slate-200 rounded-lg text-sm focus:border-amber-500 outline-none font-mono" />`;
const newLsx = `<input \n                value={form.lsx} \n                onChange={(e) => {\n                  const val = e.target.value;\n                  const newForm = { ...form, lsx: val };\n                  const matched = ALL_PHIEU.find((p: any) => p.lenhSX === val && p.mau);\n                  if (matched && matched.mau) {\n                    newForm.mau = matched.mau;\n                    if (!form.maSP) newForm.maSP = matched.maSP || "";\n                    if (!form.tenSP) newForm.tenSP = matched.phanLoai || "";\n                  }\n                  setForm(newForm);\n                }} \n                className="w-full px-3 py-2 border-2 border-slate-200 rounded-lg text-sm focus:border-amber-500 outline-none font-mono" \n              />`;

code = code.replace(oldLsx, newLsx);
code = code.replace('<label className="text-xs font-semibold text-slate-700 mb-1 block">LSX</label>', '<label className="text-xs font-semibold text-slate-700 mb-1 block">LSX (Tự động điền màu)</label>');

fs.writeFileSync(file, code);
console.log("Patched successfully");
